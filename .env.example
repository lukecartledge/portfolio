# Sanity CMS
# Dataset is public — no API token needed for reads.
# Project ID and dataset are configured in astro.config.mjs and src/lib/sanity.ts.
# SANITY_API_TOKEN=  # Only needed if dataset is changed to private
